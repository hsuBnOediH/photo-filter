# Screenshots

Place README screenshots here (referenced from the project READMEs).

Suggested set — avoid frames that show personal photos:
- `home.png` — module picker
- `organize.png` — single-group review (use expendable test photos)
- `cheatsheet.png` — the `?` overlay

Capture with `⌘⇧4` + space (window capture with shadow), or:
`screencapture -o -l <windowID> home.png`
